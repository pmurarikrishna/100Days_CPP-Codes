#include <iostream >
using namespace std;
int main(){

    int n ;
    cin>>n;
    int arra[n];
int max =0;
for (int i = 0; i < n; i++)
{
    /* code */
    cin>>arra[i];
}
for (int i = 0; i <n; i++)
{
    /* code */
    if (arra[i]>max)
    {
        /* code */
        max=arra[i];
    }
    

    // cout<<"max no is "<<max;


    
}
 cout<<"max no is "<<max;

return 0;

}


